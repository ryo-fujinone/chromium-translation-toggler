const getDefaultOptions = () => {
    return {
        triggerID: "chromium-translation-toggle-trigger",
        howToOpen: "type-keys",
        barHeight: 0,
        setBarHeight: false,
        autoGetBarHeight: false,
        firstTime: true,
    };
};

export default getDefaultOptions;
